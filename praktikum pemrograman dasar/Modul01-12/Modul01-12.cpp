// Adi Febriana (140910200022)

#include <stdio.h>
#include <conio.h>

main()
{
	int a;
	long int b; /* ini bersifat opsional */
	long long int c; /* ini bersifat opsional */
	printf("Ukuran int                  = %d bytes\n", sizeof(a));
	printf("Ukuran long int             = %1d bytes\n", sizeof(b));
	printf("Ukuran long long int        = %1d bytes", sizeof(c));
	getch();
	return 0;
	printf("Ukuran long long long int   = %d bytes", sizeof(c));
}
